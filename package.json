{
  "name": "pouchdb-activetasks-test",
  "version": "1.0.0",
  "description": "testing the PouchDB activeTasks API",
  "private": true,
  "scripts": {
    "dev": "serve --symlinks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serve": "^13.0.2"
  }
}
